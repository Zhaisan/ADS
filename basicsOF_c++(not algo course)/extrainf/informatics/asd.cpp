#include <iostream>
#include <string>
using namespace std;
int a[1000];
int main(){

    string s;
    cin >> s;
    for(int i=0;i<s.size();i++){
        a[s[i]]++;
    }
    for(char c='a';)
    return 0;
}